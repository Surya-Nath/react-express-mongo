name: build-and-push
on:
  push:
    branches: [ main ]
env:
  AWS_REGION: ap-south-1
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      - id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      - name: Build and push ui
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          SHA: ${{ github.sha }}
        run: |
          docker build -t $REGISTRY/week4-ui:$SHA -t $REGISTRY/week4-ui:latest ./frontend
          docker push $REGISTRY/week4-ui:$SHA
          docker push $REGISTRY/week4-ui:latest
      - name: Build and push api
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          SHA: ${{ github.sha }}
        run: |
          docker build -t $REGISTRY/week4-api:$SHA -t $REGISTRY/week4-api:latest ./backend
          docker push $REGISTRY/week4-api:$SHA
          docker push $REGISTRY/week4-api:latest
